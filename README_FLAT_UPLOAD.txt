FLAT GITHUB UPLOAD VERSION

This package has no folders.

Upload all files in this folder into the same GitHub directory / branch as index.html.
Do not upload the ZIP itself.

Because all assets are flat, index.html references images, videos and PDFs by filename only.
